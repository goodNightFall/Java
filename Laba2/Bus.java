package Laba2;

public class Bus extends Vehicle{
  public Bus(String brand, String model, Integer maxWeight,  Integer numberOfPassengers, Integer maxSpeed) {
    super(brand, model, maxWeight, numberOfPassengers, maxSpeed, null);
  }
}