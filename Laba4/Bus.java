package Laba4;

public class Bus extends Vehicle {
  public Bus(Integer id, String brand, String model, Integer maxWeight, Integer numberOfPassengers,
      Integer maxSpeed) {
    super(id, brand, model, maxWeight, numberOfPassengers, maxSpeed);
  }
}